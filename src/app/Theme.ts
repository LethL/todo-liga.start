import { createTheme } from '@mui/material';

export const Theme = createTheme({
  typography: {
    h1: {
      color: '#000',
      textAlign: 'center',
      textTransform: 'uppercase',
      fontSize: '2rem',
      marginBottom: '0.5em',
    },
  },
});
